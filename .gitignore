# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
*.egg-info/
dist/
build/
.venv/
venv/
env/

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# Environment (all secrets here)
.env
.env.local
.env.*.local
*.local

# Secrets directory (deprecated - use .env instead)
secrets/

# Logs
*.log
logs/

# Cache
.pytest_cache/
.mypy_cache/
.coverage
htmlcov/
arms_cache/

# Editor
.qoder/
.windsurf/

# Session files
*.session
*.session-journal

# Secrets (keep structure, not content)
secrets/*.txt
!secrets/.gitkeep

# Test data
tests/test_messages.txt

# Documentation: markdown (README.md, docs/, CLAUDE.md) is tracked by default.
# Do NOT blanket-ignore *.md and re-allow with `!` negations — many copy/export
# tools that honor .gitignore drop the ignored files but skip the `!` rules,
# silently leaving README.md & docs out of the copy. Ignore specific scratch
# markdown by name instead, e.g.:  /REVIEW.md
pg_plan3.txt

# Archived migration scripts and docs
postgres/migrations/ARCHIVE/
docs/ARCHIVE/

# Misc
.DS_Store
Thumbs.db


# Frontend build artifacts
web/node_modules/
web/dist/

# Runtime media uploads
media/
logs
# Build / profiling artifacts
optimization_results.json

# Generated CSV exports (large, produced locally — not versioned)

#!postgres/data/*.csv

# IDE / tool config
.devin/
.kilo/
review.md

# ONNX model files (large binaries, downloaded separately, not versioned)
parser/models/
processor/models/rubert-tiny2-onnx/model.onnx
processor/models/rubert-tiny2-onnx/tokenizer.json
processor/models/rubert-tiny2-onnx/vocab.txt
processor/models/rubert-tiny2-onnx/config.json
processor/models/rubert-tiny2-onnx/ort_config.json


