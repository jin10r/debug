# NER training dependencies — Windows + NVIDIA GPU (GTX 1050, Pascal CC 6.1).
#
# IMPORTANT: install torch FIRST from the CUDA wheel index (NOT from PyPI, which
# gives the CPU build). See README.md step 3. Example:
#   pip install "torch==2.5.1" --index-url https://download.pytorch.org/whl/cu121
#
# Then install the rest:
#   pip install -r requirements-gpu.txt
#
# (torch is intentionally omitted here so pip doesn't pull the CPU wheel.)

transformers>=4.44,<5
datasets>=2.20
seqeval>=1.2.2
accelerate>=0.33
optimum[onnxruntime]>=1.21
onnxruntime>=1.18
tokenizers>=0.19
numpy>=1.26,<2.1
