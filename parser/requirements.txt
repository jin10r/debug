# Parser requirements - Survival Map v4 (Sliding Window + Rapidfuzz)
# CPU-only entity matching with rapidfuzz

# Telegram client (using fixed version)
kurigram==2.2.9
tgcrypto>=1.2.0
pysocks>=1.7.0          # SOCKS proxy support for Pyrogram

# Database
asyncpg>=0.29.0          # PostgreSQL client

# Fuzzy search - Sliding Window algorithm
rapidfuzz>=3.5.0         # Fast fuzzy string matching

# Configuration
environs>=11.0.0         # Environment variables

# HTTP server (for health checks)
aiohttp>=3.9.0
aiohttp-cors>=0.7.0

# Monitoring
prometheus-client>=0.19.0  # Metrics
tenacity>=8.2.0            # Retry logic
pybreaker>=1.3.0           # Circuit breaker

# Utilities
python-json-logger>=2.0.0  # Structured logging
pytz>=2024.1               # Timezone conversions (UTC → Europe/Kiev)

# Array operations
numpy>=1.24.0,<2.0

# Progress bars
tqdm>=4.65.0
