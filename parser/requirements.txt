# Telegram client (kurigram — поддерживаемый форк pyrogram)
kurigram==2.2.9
tgcrypto>=1.2.0

# Database
asyncpg>=0.29.0          # PostgreSQL client

# Морфология + токенизация (mawo-экосистема)
mawo-pymorphy3==1.0.4    # Russian morphological analyzer with DAWG (~15-20 MB RAM)
mawo-razdel>=1.0.6       # Russian tokenizer/sentenizer (аббревиатуры, инициалы)

# Русская фонетика — Metaphone (parser/phonetic_index.py).
# fonetika.metaphone.RussianMetaphone: оглушение/редукция/-его→-ево.
# Импортируем только .metaphone — peer pymorphy2 не загружается в рантайме.
fonetika>=1.5.1

# Фуззи-матч против alias-индекса
rapidfuzz>=3.0.0         # Fast fuzzy string matching (C implementation, SIMD)

# Кастомный NER (parser/ner_matcher.py) — CPU-инференс LOC-спанов без torch.
# rubert-tiny2 экспортируется в ONNX (training/export_onnx.py); грузится через
# onnxruntime + tokenizers. Лёгкие колёса (~20 MB), на порядок легче torch.
onnxruntime>=1.18
tokenizers>=0.19
numpy>=1.26

# Configuration (через core.settings)
environs>=11.0.0

# Timezone data — stdlib zoneinfo использует этот пакет как источник данных
# о часовых поясах (базовый образ python:3.11-slim системную базу не содержит).
tzdata>=2024.1
