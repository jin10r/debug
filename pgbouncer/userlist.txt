; =============================================================================
; PgBouncer userlist — format: "username" "md5<md5(password+username)>"
; =============================================================================
; This file is overridden at container startup by the entrypoint script.
; The entrypoint reads POSTGRES_USER and POSTGRES_PASSWORD from env vars
; and generates the md5 hash automatically.
;
; Manual generation (for debugging):
;   echo -n "passwordpostgres" | md5sum | awk '{print "\"postgres\" \"md5"$1"\""}'
;
; DO NOT commit real passwords to git.
