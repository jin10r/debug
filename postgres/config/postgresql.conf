jit = off

listen_addresses = '*'

# pg_cron: must be preloaded so the 'cron.*' GUCs are registered at startup,
# which allows CREATE EXTENSION pg_cron and cron.schedule() to work.
shared_preload_libraries = 'pg_cron'
cron.database_name = 'postgres'
