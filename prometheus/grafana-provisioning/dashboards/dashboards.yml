# Grafana dashboards provider provisioning
apiVersion: 1

providers:
  - name: 'Survival Map'
    orgId: 1
    folder: 'Survival Map'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards
