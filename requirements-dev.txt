# Dev/test dependencies. Install on top of requirements.txt + parser/requirements.txt
# to run the full suite (some tests are skipped if heavy runtime deps are absent).
#   pip install -r requirements.txt -r parser/requirements.txt -r requirements-dev.txt
#   pytest
pytest>=8,<10
