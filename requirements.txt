# Main application dependencies
#
# Пиннинг — compatible-release (~=): фиксирует major.minor, пускает только
# patch/minor в пределах major. Это убирает риск «каждая сборка тянет разную
# major-версию», но НЕ даёт побайтовой воспроизводимости. Для полного лока
# снять `pip freeze` из собранного образа и закоммитить как requirements.lock
# (см. CI-шаг lock-check в .gitlab-ci.yml).
aiohttp~=3.9
aiogram~=3.4
asyncpg~=0.29
aiohttp-cors~=0.7
environs~=11.0
prometheus-client~=0.19
pybreaker~=1.3
pydantic~=2.0          # core/models.py, core/api/events.py — раньше тянулся только
                       # транзитивно через aiogram; теперь объявлен явно.

# Authentication & session management
pyjwt~=2.8
redis~=5.0
# cryptography нужен pyjwt только для RSA/EC-алгоритмов. Текущий JWT — HS256
# (core/settings.py), прямых импортов нет → кандидат на удаление. Оставлен
# закреплённым на случай переключения на RS256; убрать, если не планируется.
cryptography~=42.0
