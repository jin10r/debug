# Main application dependencies — exact pins for reproducible builds.
# Regenerate via: pip freeze > requirements.lock (see CI step lock-check).
aiohttp==3.14.3
aiogram==3.30.0
asyncpg==0.30.0
aiohttp-cors==0.7.0
environs==11.2.1
orjson>=3.9.0
prometheus_client>=0.19.0
pybreaker==1.4.1
pydantic==2.13.4
pyjwt==2.13.0
