# Main application dependencies — exact pin (==) for reproducible builds
#
# Для обновления: pip index versions <pkg> → выбрать стабильную версию →
# обновить строку ниже. НЕ использовать ~= (compatible-release): он даёт
# разные minor/patch при каждой сборке.
aiohttp==3.14.1
aiogram==3.29.1
asyncpg==0.31.0
aiohttp-cors==0.8.1
environs==15.0.1
prometheus-client==0.25.0
pybreaker==1.4.1
pydantic==2.13.4

# Authentication & token signing
pyjwt==2.13.0
# cryptography НЕ требуется: JWT — HS256 (hmac/hashlib из stdlib), прямых импортов
# нет. При переходе на RS256/ES256 поставить `pyjwt[crypto]` (подтянет cryptography).
